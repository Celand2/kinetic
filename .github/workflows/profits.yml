name: Créditer les profits journaliers
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  credit-profits:
    runs-on: ubuntu-latest
    steps:
      - name: Appeler le webhook
        run: |
          curl -f "https://kts-app.com/webhook-profits.php?token=${{ secrets.WEBHOOK_TOKEN }}"
